{
  "name": "clicker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build-css-watch": "tailwindcss build -i src/style.css -o public/output.css --watch",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d public"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "tailwind": "^4.0.0",
    "vite": "^4.0.0"
  }
}
